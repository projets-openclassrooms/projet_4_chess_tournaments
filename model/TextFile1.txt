# Create an instance of the TournamentManager class
manager = TournamentManager()

# Create a tournament with a name, location, and list of players
tournament = manager.create_tournament(players_saved)

# Set the tournament property of the manager to the created tournament
manager.tournament = tournament

# Create the first turn of the tournament
first_turn = manager.define_first_turn()

# Print the details of the first turn
print(first_turn.name)
print(first_turn.turn)
print(first_turn.match_list)
